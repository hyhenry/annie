# Annie — Stock Recommendation Engine
# Python dependencies
#
# Install with: pip install -r requirements.txt
# Requires Python 3.9+

# ── Market data (default path — no API key required) ──────────────────────────

# Yahoo Finance data fetcher — downloads OHLCV history for any US stock, free
yfinance>=0.2.40

# Technical indicator library — computes RSI, MACD, BB, Stoch, ATR, ADX, OBV, etc.
ta>=0.11.0

# ── Web API ───────────────────────────────────────────────────────────────────

# FastAPI — REST API framework that replaces Streamlit
fastapi>=0.111.0

# Uvicorn — ASGI server to run FastAPI
uvicorn[standard]>=0.29.0

# ── Core ──────────────────────────────────────────────────────────────────────

# Loads environment variables from a .env file into os.environ
python-dotenv==1.0.0

# Pandas — used for DataFrames, universe data, resampling
pandas>=2.0.0

# lxml — required by pandas.read_html to parse Wikipedia tables (universe/sector data)
lxml>=4.9.0

# ── Testing ───────────────────────────────────────────────────────────────────

# Test runner
pytest==7.4.3

# Code coverage reporting (optional but recommended)
pytest-cov==4.1.0
